import AppBar from '@mui/material';

const Header = () => {
  return (
    <AppBar position="static">
      <AppBarTitle>My Website</AppBarTitle>
    </AppBar>
  );
};

export default Header;